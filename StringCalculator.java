// --- StringCalculator minimal implementation ---
class StringCalculator {
    public int add(String numbers) {
        if (numbers == null || numbers.isEmpty()) {
            return 0;
        }
        // To be extended for further requirements
        return 0;
    }
}
